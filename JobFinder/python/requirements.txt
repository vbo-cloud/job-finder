# TODO: pin all versions before first deployment (run pip freeze > requirements.txt)
sqlalchemy>=2.0
psycopg2-binary
pgvector
azure-identity
azure-monitor-opentelemetry==1.8.8
azure-servicebus
azure-storage-blob
openai
alembic
pdfplumber
pypdfium2
python-dotenv
requests
structlog
# zoneinfo (stdlib) needs the IANA tz database to resolve "Europe/Paris" — missing on
# python:3.12-slim, which these agents deploy from (agents/offer_fetching/main.py).
tzdata
