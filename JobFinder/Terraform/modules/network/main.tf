# Module: network (placeholder)
# Will provision reusable VNet and subnet components.
# Currently empty — network resources are defined inline in lz_dev and lz_prod
# because the hub-and-spoke topology is still single-environment.
# Move the VNet/subnet blocks here once network config needs to be shared
# across environments or extended with peering, NSGs, or route tables.
